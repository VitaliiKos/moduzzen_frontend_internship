version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${REACT_APP_NGINX_PORT}:${REACT_APP_DOCKER_PORT}"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: on-failure

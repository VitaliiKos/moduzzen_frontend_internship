version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: on-failure
